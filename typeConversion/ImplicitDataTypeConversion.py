# Implicit conversion or coercion is when data type conversion takes place either during compilation
# or during run time and is handled directly by Python

a_int = 1
b_float = 1.0
c_sum = a_int + b_float
print(c_sum)
print(type(c_sum))